module.exports = {
    mobileMaxWidth:    699,
    removeAppTimeout: 1000,
    showAppTimeout:   2000,
    resizeTimeout:       0,

    elements: {
        loader: 'app-loader',
        app:    'app'
    }
};